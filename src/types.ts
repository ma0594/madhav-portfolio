export interface SkillItem {
  id: string;
  badge: string;
  badgeType: string;
  title: string;
  description: string;
  highlight: string;
  iconName: string;
}

export interface CourseworkItem {
  title: string;
  description: string;
  iconName: string;
}

export interface ProjectArchitectureModule {
  id: string;
  title: string;
  description: string;
  tag: string;
}
